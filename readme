
install pip first 
sudo apt-get install python3-pip
sudo apt-get update

run the following commands on linux terminal to install the needed libraries

sudo apt-get install python3-pandas
pip install networkx 
pip install seaborn
pip install matplotlib